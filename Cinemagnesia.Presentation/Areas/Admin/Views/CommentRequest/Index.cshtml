
    <h1>User Comments</h1>

    <div class="row">
        <div class="col-md-6">
            <h2>Requests</h2>
            <div class="list-group">
              @*@foreach (var comment in Model.Where(c => !c.IsRejected && !c.IsApproved))
                        {
                            <div class="list-group-item">
                                <div class="row">
                                    <div class="col-md-8">
                                        <h4 class="list-group-item-heading">@comment.UserName</h4>
                                        <p class="list-group-item-text">@comment.CommentText</p>
                                        <<small>@comment.CommentedMovie</small>
                                    </div>
                                    <div class="col-md-4">
                                        <div class="btn-group">
                                            <form asp-action="Approve" asp-route-commentId="@comment.Id" method="post">
                                                <div class="form-check d-inline-block mr-2">
                                                    <input type="checkbox" class="form-check-input" id="hasSpoiler_@comment.Id" name="HasSpoiler" value="true" @if (comment.HasSpoiler) { <text>checked</text> }>
                                                    <label class="form-check-label" for="hasSpoiler_@comment.Id">Spoier İçeriyor</label>
                                                </div>
                                                <button type="submit" class="btn btn-primary d-inline-block mr-2">Kabul Et</button>
                                            </form>
                                            <form asp-action="Reject" asp-route-commentId="@comment.Id" method="post">
                                                <button type="submit" class="btn btn-danger d-inline-block">Reddet</button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }*@
                    <div class="list-group-item">
                            <h4 class="list-group-item-heading">RandomPerson</h4>
                            <p class="list-group-item-text">AJLKDJKLASDFLJŞKFADSJLKŞFADSJLKŞDFASJLKŞFASDJKLFASDJKLŞFADSJŞLKDFASJKLŞ</p>
                            <small>Avatar</small>
                    </div>
                    <div class="btn-group">
                        <form asp-action="Approve" asp-route-commentId="" method="post">
                            <div class="form-check form-check-inline">
                                <input type="checkbox" class="form-check-input" id="hasSpoiler" name="HasSpoiler" value="true" @if (true)
                                {
                                    <text>checked</text>
                                }>
                                <label class="form-check-label" for="hasSpoiler">Has Spoiler</label>
                            </div>
                            <button type="submit" class="btn btn-primary">Approve</button>
                        </form>
                        <form asp-action="Reject" asp-route-commentId="" method="post">
                            <button type="submit" class="btn btn-danger">Reject</button>
                        </form>
                    </div>

                    <div class="list-group-item">
                                <h4 class="list-group-item-heading">Lorem ipsum</h4>
                                <p class="list-group-item-text">Lorem ipsum dolor sit amet, consectetur adipiscing elit. Fusce non metus vel ex maximus sagittis. Nulla facilisi. Sed gravida convallis nisi vel interdum. Etiam faucibus suscipit bibendum. Integer varius neque et erat sollicitudin luctus. Duis laoreet urna elit, at pulvinar nisl malesuada sed. Aenean feugiat massa ac est pellentesque, ac tempor justo dignissim.</p>
                                <small>Lorem ipsum dolor sit amet</small>
                    </div>
                    <div class="btn-group">
                         <form asp-action="Approve" asp-route-commentId="123" method="post">
                              <div class="form-check form-chech-inline">
                                  <input type="checkbox" class="form-check-input" id="hasSpoiler_123" name="HasSpoiler" value="true" checked>
                                  <label class="form-check-label" for="hasSpoiler_123">Has Spoiler</label>
                              </div>
                                    <button type="submit" class="btn btn-primary">Approve</button>
                                </form>
                                <form asp-action="Reject" asp-route-commentId="123" method="post">
                                    <button type="submit" class="btn btn-danger">Reject</button>
                                </form>
                    </div>
                        
                    

           </div>

                    
                

    </div>
        
        <div class="col-md-6">
        <h2>Approved Comments <a href="@Url.Action("ApprovedRequests", "Admin")" class="btn btn-primary">View All</a></h2>
        
        
            <div class="list-group">
           
               @* @foreach (var comment in Model.Where(c => c.IsApproved))
                {
                    <div class="list-group-item">
                        <h4 class="list-group-item-heading">@comment.UserName</h4>
                        <p class="list-group-item-text">@comment.CommentText</p>
                        @if (comment.HasSpoiler)
                        {
                            <button class="btn btn-info">Spoilerlı Mesajı Göster</button>
                            <div style="display: none;">
                                <p>@comment.CommentText</p>
                            </div>
                        }
                        <small>@comment.CommentedMovie</small>
                    </div>
                }*@
            <div class="list-group-item">
                <h4 class="list-group-item-heading">Filmkolik</h4>
                @if (true)
                {
                    <button type="button" class="btn btn-warning" data-toggle="collapse" data-target="#spoiler-Id">Spoilerlı mesajı göster</button>
                    <div id="spoiler-Id" class="collapse">
                        <p class="list-group-item-text">Güzel filmdi sonunda yaratığın ölmesini görmek isterdim.</p>
                    </div>
                }
                else
                {
                    <p class="list-group-item-text">Güzel filmdi sonunda yaratığın ölmesini görmek isterdim.</p>
                }
                <small>Cloverfield</small>
            </div>

            </div>
        <h2>Rejected Comments <a href="@Url.Action("RejectedRequests", "Admin")" class="btn btn-primary">View All</a></h2>
        
            <div class="list-group">

               @* @foreach (var comment in Model.Where(c => c.IsRejected))
                {
                    <div class="list-group-item">
                        <h4 class="list-group-item-heading">@comment.UserName</h4>
                        <p class="list-group-item-text">@comment.CommentText</p>
                        <small>@comment.CommentedMovie</small>
                    </div>
                }*@
            <div class="list-group-item">
                <h4 class="list-group-item-heading">Agresif-Reis</h4>
                @if (false)
                {
                    <button type="button" class="btn btn-warning" data-toggle="collapse" data-target="#spoiler-comment.Id">Reveal</button>
                    <div id="spoiler-Id" class="collapse">
                        <p class="list-group-item-text">CommentText</p>
                    </div>
                }
                else
                {
                    <p class="list-group-item-text">Bu nasıl film #$!#$! bu filme güzel diyenler de herhalde ayrı ½$#!$ sakın izlemeyin</p>
                }
                <small>Cloverfield</small>
            </div>

          
            </div>
        </div>
    </div>

@*@if (Model.Where(c => !c.IsRejected && !c.IsApproved).Count() > 5)
{
    <button type="button" class="btn btn-primary" id="show-more-button">Show More</button>
}*@

@if (true)
{
    <button type="button" class="btn btn-primary" id="show-more-button">Show More</button>
}

@section Scripts {
    <script>
        $(function () {
            $("#show-more-button").click(function () {
                // Load and display the additional comments
                // ...
            });
        });
    </script>
}