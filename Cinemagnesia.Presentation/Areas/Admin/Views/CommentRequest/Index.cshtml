
    <h1>User Comments</h1>

    <div class="row">
        <div class="col-md-6">
            <h2>Requests</h2>
            <div class="list-group">
               @*@foreach (var comment in Model.Where(c => !c.IsRejected && !c.IsApproved))
                    {
                        <div class="list-group-item">
                            <h4 class="list-group-item-heading">@comment.UserName</h4>
                            <p class="list-group-item-text">@comment.CommentText</p>
                            <small>@comment.CommentedMovie</small>
                            <div class="btn-group">
                                <form asp-action="Approve" asp-route-commentId="@comment.Id" method="post">
                                    <div class="form-check">
                                        <input type="checkbox" class="form-check-input" id="hasSpoiler" name="HasSpoiler" value="true" @if (comment.HasSpoiler) { <text>checked</text> }>
                                        <label class="form-check-label" for="hasSpoiler">Has Spoiler</label>
                                    </div>
                                    <button type="submit" class="btn btn-primary">Approve</button>
                                </form>
                                <form asp-action="Reject" asp-route-commentId="@comment.Id" method="post">
                                    <button type="submit" class="btn btn-danger">Reject</button>
                                </form>
                                <form asp-action="Spoiler" asp-route-commentId="@comment.Id" method="post">
                                    <button type="submit" class="btn btn-warning">Add Spoiler</button>
                                </form>
                            </div>
                        </div>
                    }*@
                    <div class="list-group-item">
                            <h4 class="list-group-item-heading">UserName</h4>
                            <p class="list-group-item-text">CommentText</p>
                            <small>CommentedMovie</small>
                            <div class="btn-group">
                                <form asp-action="Approve" asp-route-commentId="" method="post">
                                    <div class="form-check">
                                        <input type="checkbox" class="form-check-input" id="hasSpoiler" name="HasSpoiler" value="true" @if (comment.HasSpoiler) { <text>checked</text> }>
                                        <label class="form-check-label" for="hasSpoiler">Has Spoiler</label>
                                    </div>
                                    <button type="submit" class="btn btn-primary">Approve</button>
                                </form>
                                <form asp-action="Reject" asp-route-commentId="" method="post">
                                    <button type="submit" class="btn btn-danger">Reject</button>
                                </form>
                                <form asp-action="Spoiler" asp-route-commentId="" method="post">
                                    <button type="submit" class="btn btn-warning">Add Spoiler</button>
                                </form>
                            </div>
                        </div>
            </div>
        </div>
        <div class="col-md-6">
            <h2>Approved Comments</h2>
            <div class="list-group">
               @* @foreach (var comment in Model.Where(c => c.IsApproved))
                {
                    <div class="list-group-item">
                        <h4 class="list-group-item-heading">@comment.UserName</h4>
                        <p class="list-group-item-text">@comment.CommentText</p>
                        @if (comment.HasSpoiler)
                        {
                            <button class="btn btn-info">Reveal</button>
                            <div style="display: none;">
                                <p>@comment.CommentText</p>
                            </div>
                        }
                        <small>@comment.CommentedMovie</small>
                    </div>
                }*@
            <div class="list-group-item">
                <h4 class="list-group-item-heading">Filmkolik</h4>
                @if (false)
                {
                    <button type="button" class="btn btn-warning" data-toggle="collapse" data-target="#spoiler-Id">Reveal</button>
                    <div id="spoiler-Id" class="collapse">
                        <p class="list-group-item-text">CommentText</p>
                    </div>
                }
                else
                {
                    <p class="list-group-item-text">Güzel filmdi izlemenizi tavsiye ederim.</p>
                }
                <small>Cloverfield</small>
            </div>

            </div>
            <h2>Rejected Comments</h2>
            <div class="list-group">
               @* @foreach (var comment in Model.Where(c => c.IsRejected))
                {
                    <div class="list-group-item">
                        <h4 class="list-group-item-heading">@comment.UserName</h4>
                        <p class="list-group-item-text">@comment.CommentText</p>
                        <small>@comment.CommentedMovie</small>
                    </div>
                }*@
            <div class="list-group-item">
                <h4 class="list-group-item-heading">Agresif-Reis</h4>
                @if (false)
                {
                    <button type="button" class="btn btn-warning" data-toggle="collapse" data-target="#spoiler-comment.Id">Reveal</button>
                    <div id="spoiler-Id" class="collapse">
                        <p class="list-group-item-text">CommentText</p>
                    </div>
                }
                else
                {
                    <p class="list-group-item-text">Bu nasıl film #$!#$! bu filme güzel diyenler de herhalde ayrı ½$#!$ sakın izlemeyin</p>
                }
                <small>Cloverfield</small>
            </div>

          
            </div>
        </div>
    </div>
